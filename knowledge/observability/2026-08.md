# Observabilidade — 2026-08

## 2026-08-11 — Gatilho imediato: task_type "Épico" vs "Epic"

```yaml
date: 2026-08-11
mission: outro
skills: [orquestrador, clickup-spec]
result: artifact
blocker: true
rework: true
human_intervention: true
artifact: knowledge/decisions/2026-08-11-gatilho-imediato-observabilidade-doc-desatualizada.md
```

`agente-delivery` tentou gravar `task_type = "Épico"` (nome funcional em português) num Projeto de Delivery; a API do workspace só aceita `"Epic"` (inglês). A chamada falhou por completo (nem tipo nem descrição foram gravados) e o PM precisou identificar o erro e reenviar manualmente. Rastreado a doc/skill referenciada (não a julgamento livre do agente) — qualifica para o gatilho imediato de `services/observability.md`.

Fast-track aplicado: corrigidas 24 ocorrências do mesmo valor em 9 arquivos, em 3 árvores de runtime mantidas em paralelo (`.claude/`, `.agents/skills/`, `.codex/agents/`) — ver addendum na decisão. Achado estrutural (falta de sincronização entre as 3 árvores) sinalizado como candidato a proposta do `agente-evolucao`, fora do escopo deste fast-track.

## 2026-08-11 — Gatilho imediato: drift do nível Iniciativa (removido do processo, mas ainda ativo no checklist)

```yaml
date: 2026-08-11
mission: especificar
skills: [orquestrador, clickup-spec]
result: artifact
blocker: false
rework: true
human_intervention: true
artifact: knowledge/decisions/2026-08-11-remocao-drift-iniciativa-checklist-clickup.md
```

`agente-delivery` reportou "campo Iniciativa vazio" como gap de qualidade na validação de `868kpcu41`, apesar
de `knowledge/domains/processo.md` já registrar que o nível Iniciativa foi removido do processo. Causa raiz:
a remoção nunca se propagou a `clickup-config.md` (fonte de IDs/regras que a skill carrega), que continuava
tratando a lista Iniciativas como ativa e instruindo a gravar campos de priorização (Impacto/Alcance/Score/
Horizonte/Votos) que, confirmado via dado real, nunca existiram em Discovery/Delivery. Rastreado a doc/skill
referenciada — qualifica para o gatilho imediato.

Fast-track aplicado: corrigidas as ocorrências em `clickup-config.md`, `clickup-config-tech.md` (arquivo
órfão, marcado legado), `template-delivery.md`, `agente-delivery.md` (contradição interna entre linhas 43 e
90) e `clickup-method.md`. Na mesma rodada, o PM pediu uma segunda correção de processo (não é fast-track —
é regra nova): a seção "Aberto para refinamento" foi renomeada para "Aberto para refinamento técnico" e
restrita a decisão técnica de engenharia; pendência de produto/compliance passa a ser triada pelo Orquestrador
com o agente especialista certo antes do card — ver
[2026-08-11-aberto-para-refinamento-tecnico-restricao-de-uso.md](../decisions/2026-08-11-aberto-para-refinamento-tecnico-restricao-de-uso.md).
Pergunta aberta, não resolvida aqui: onde os campos de priorização (Impacto/Alcance/Score) devem viver agora
que a Iniciativa não existe — candidato a proposta do `agente-evolucao`.

## 2026-08-11 — Geração automática de .codex/ e .agents/ implementada

```yaml
date: 2026-08-11
mission: outro
skills: [orquestrador]
result: artifact
blocker: false
rework: false
human_intervention: false
artifact: knowledge/decisions/2026-08-11-geracao-automatica-runtimes-codex-agents.md
```

Candidato de investigação sinalizado no addendum da decisão do `task_type` foi levado ao fim: `agente-evolucao`
investigou (achou também que `.codex/`/`.agents/` nunca haviam sido commitados — `install.sh` estava
confirmadamente quebrado para instalação remota do Codex) e `agente-governanca` aprovou com 4 ressalvas
(R1: hook automático rebaixado a comando manual; R2: sequenciamento — o usuário optou por implementar já,
contrariando a ressalva, por já considerar o conteúdo pronto; R3: skills novas ficam fora do primeiro commit
até revisão própria; R4: parser precisa falhar alto em colisão de `"""`).

Implementado: `scripts/build-runtimes.js` (gerador zero-deps, com prune de órfãos e validação R4), `.codex/`
removido do `.gitignore`, `services/runtime-sync.md` (novo), regeneração defensiva em `install.sh`. Gerador
rodado uma vez — `.codex/agents/*.toml` e `.agents/skills/clickup-spec/references/**` já refletem o estado
atual de `.claude/` (incluindo a remoção do nível Iniciativa, registrada na entrada acima). Commit ainda não
feito — decisão do usuário, fora do escopo desta entrada.

## 2026-08-12 — Gatilho imediato: nomes de status da esteira Execução (Experiência do jogador) divergentes da doc

```yaml
date: 2026-08-12
mission: especificar
skills: [clickup-spec]
result: artifact
blocker: true
rework: true
human_intervention: false
artifact: null
```

`agente-delivery` recebeu instrução do Orquestrador (a pedido do PM) para mover um bug ativo de produção
(`868kqjgkt`) direto de Backlog para o primeiro status da esteira de Execução — squad Experiência do jogador,
folder "Delivery: Plataforma" (`901114029785`). `clickup_update_task` com `status: "to do"` falhou
(`Status does not exist`): o fluxo real dessa lista, confirmado via `clickup_get_task` com `expand_statuses`,
é `pendente → desenvolvimento → bloqueada → revisão técnica → teste em dev → deploy p/ alpha → teste em alpha
→ deploy p/ prd → cancelado/finalizado` — nada em comum com o `to do → em desenvolvimento → ... → aguardando
deploy → ...` documentado em `clickup-config.md` e `knowledge/domains/processo.md` (que tratavam Experiência
do jogador e Operação e afiliados como um fluxo único). Rastreado a doc referenciada, não a inferência livre
do agente — qualifica para o gatilho imediato.

Fast-track aplicado: corrigidos `clickup-config.md` (linha da lista Execução — Experiência do jogador, com
data de confirmação; a linha de Operação e afiliados foi separada e marcada como não reverificada, já que a
suposição de fluxo compartilhado entre as duas squads se mostrou falsa) e `knowledge/domains/processo.md`
(removida a alegação de "Status (Execução)" único, com nota de que diverge por squad). `scripts/build-runtimes.js`
rodado para propagar a correção a `.agents/skills/clickup-spec/references/clickup-config.md`. O card em si não
ficou bloqueado — `pendente` já era o status padrão de entrada da lista, então o `move_task` já havia deixado
o card no lugar certo antes mesmo da correção; o retrabalho foi só na chamada de `update_task`.

Achado adicional na mesma tarefa, não corrigido aqui (falta evidência para editar com segurança): o campo
`_Classe`, listado em `clickup-method.md` como obrigatório para Delivery, não aparece entre os custom fields
reais de nenhuma das duas listas (Backlog/Execução) do folder "Delivery: Plataforma" verificadas — candidato a
nova investigação do `agente-evolucao`, não generalizei a partir de uma squad só. Também apareceu um campo
`MoSCoW` (Must/Should/Could/Would Have) na lista Execução que não existe na lista Backlog e não está
documentado em nenhuma referência da skill — sinalizado ao PM no handoff da missão, não preenchido.

## 2026-08-14 — Gatilho imediato: "Módulo do PAM" e demais campos do Espaço não são herdados uniformemente

```yaml
date: 2026-08-14
mission: especificar
skills: [orquestrador, clickup-spec]
result: artifact
blocker: false
rework: false
human_intervention: false
artifact: null
```

Ao estruturar o épico "Expandir o catálogo de jogos da Tradicional com novos provedores via integração PNP"
(`868kapw4g`, folder "Delivery: Operação e afiliados" / lista Backlog `901114029782`), `agente-delivery` foi
setar o campo obrigatório `Módulo do PAM` (`f02314ee-…`) conforme `clickup-config.md` → "Custom Fields —
Espaço (herdados por todas as listas)", que afirmava esse campo (junto com `Empresa`, `MoSCoW`, `_Projeto`)
herdado do Espaço por toda lista de Discovery/Delivery. `clickup_get_custom_fields` com `list_id` + `folder_id`
+ `space_id` + `include_workspace` na lista alvo retornou só `KPIs` e o campo vestigial `Iniciativa` — nenhum
dos 4 campos aparece em nenhum escopo. Confirmado também nos custom fields reais de dois cards dessa lista
(`868kapw4g`, `868kf0wep`, via `clickup_get_task` com `include: ["custom_fields"]`): nenhum tem os 4 campos.
Por comparação, o card real `868kpcu41` (lista Execução, folder "Delivery: Plataforma") citado como evidência
na própria doc **tem** os 4 — ou seja, a doc generalizou a partir de uma squad só (mesmo padrão do achado de
2026-08-11 sobre `_Classe`/`MoSCoW` acima, não reconciliado até agora). Rastreado a doc referenciada, não a
inferência livre do agente — qualifica para o gatilho imediato.

Fast-track aplicado: reescrita a seção em `clickup-config.md` (renomeada para deixar explícito que a presença
varia por lista, com o resultado da verificação de cada campo nas duas listas comparadas) e propagado via
`node scripts/build-runtimes.js`. O card `868kapw4g` foi criado sem `Módulo do PAM` — o gap foi sinalizado no
handoff da missão ao PM em vez de bloquear a criação ou inventar um valor. Não fiz a reconciliação completa
(`_Classe` continua pendente desde 2026-08-11; presença de `MoSCoW`/`_Projeto`/`Empresa` em outras listas de
Delivery além das duas comparadas aqui segue não verificada) — candidato a levantamento único e definitivo do
`agente-evolucao` por lista, em vez de cada agente redescobrir isso incidentalmente card a card.

## 2026-08-17 — Terceira confirmação: `_Classe` e `_Risco Reg.` não existem no folder "Delivery: Plataforma"

```yaml
date: 2026-08-17
mission: especificar
skills: [clickup-spec]
result: no-conclusion
blocker: false
rework: false
human_intervention: false
artifact: null
```

Ao estruturar a descrição e os custom fields da tarefa `868kt10cv` (lista Execução, folder "Delivery:
Plataforma", `901114029785`), `clickup_get_custom_fields` (list_id + folder_id) e o próprio `clickup_get_task`
confirmaram, pela terceira vez neste mesmo folder (após 2026-08-12 e 2026-08-14 acima), que `_Classe` e
`_Risco Reg.` — listados como campos obrigatórios de Delivery em `clickup-method.md` (linhas ~112-115) e
citados em `agente-delivery.md` (regra de risco regulatório) — não existem entre os custom fields reais da
lista. O agente não inventou os campos: seguiu preenchendo só o que existe (`Módulo do PAM`, `KPIs`, `MoSCoW`,
`Solicitante`) e sinalizou a ausência no handoff ao PM.

Não fiz fast-track da doc nesta missão — mantendo a mesma linha das duas ocorrências anteriores, que já
julgaram a correção como estrutural (qual tabela/arquivo vira fonte única para o enum de campos obrigatórios
por nível, não só um valor divergente) e não trivial. Com três confirmações no mesmo folder, a evidência para
`agente-evolucao` propor a reconciliação definitiva (remover `_Classe`/`_Risco Reg.` da tabela ou aplicar a
mesma ressalva "presença varia, confirme antes de usar" já usada para `Empresa`/`Módulo do PAM`) está mais
forte — candidato a priorizar no próximo passe, não mais uma investigação nova do zero.

## 2026-08-17 — Gatilho imediato: a "terceira confirmação" de ausência de `_Classe` estava errada — o campo existe

```yaml
date: 2026-08-17
mission: especificar
skills: [clickup-spec]
result: artifact
blocker: false
rework: true
human_intervention: false
artifact: .claude/skills/clickup-spec/references/clickup-config.md
```

Poucas horas depois da entrada acima (mesmo dia, mesma lista Execução do folder "Delivery: Plataforma"),
`agente-delivery` foi setar `_Classe = "Solicitação de Mudança"` na criação da task `868kt2hdd` (a pedido
explícito do PM, mesma classificação usada em `868kmj13a`). Em vez de confiar de novo em
`clickup_get_custom_fields`, tentei a prova mais forte possível: escrever o valor via `custom_fields` em
`clickup_create_task` e reler a task criada via `clickup_get_task` com `include: ["custom_fields"]`. O campo
**existe** e o valor persistiu (`value: 4`, "Solicitação de Mudança") — o mesmo já era visível no card real
`868kmj13a`, que eu tinha lido momentos antes. Ou seja: as três "confirmações de ausência" anteriores
(2026-08-11, 2026-08-14, e a entrada de hoje mais cedo) usaram um método que não prova ausência —
`clickup_get_custom_fields` (list_id+folder_id) e a leitura de uma task específica sem esse campo preenchido
podem simplesmente não exibir um campo que existe. Rastreado a doc referenciada (a própria entrada de
observabilidade de hoje mais cedo, que eu ia repetir por padrão) — qualifica para o gatilho imediato; é uma
correção factual trivial (o campo existe/não existe, fato comprovável por escrita), não uma decisão
estrutural, então apliquei o fast-track sem esperar `agente-governanca`.

Fast-track aplicado: `clickup-config.md` → seção "Custom Fields — Espaço" ganhou a entrada de `_Classe` com
o ID do campo, as opções e a prova por escrita, revogando explicitamente a conclusão da entrada anterior de
hoje. **Não** estendi a mesma conclusão a `_Risco Reg.` — não testei esse campo por escrita nesta missão, ele
continua com status incerto. Recomendo ao `agente-evolucao`, ao revisar o candidato apontado na entrada
anterior, testar `_Risco Reg.` pelo mesmo método (escrita + releitura) antes de decidir entre remover da
tabela de obrigatórios ou apenas marcar "presença varia" — não repetir só `clickup_get_custom_fields`.
