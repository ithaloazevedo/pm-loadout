# Observabilidade — 2026-09

## 2026-09-02 — Fluxo de status errado (não só "não verificado") na lista Execução de Operação e afiliados

```yaml
date: 2026-09-02
mission: especificar
skills: [clickup-spec]
result: artifact
blocker: false
rework: false
human_intervention: false
artifact: https://app.clickup.com/t/868m0b3ph
```

Ao criar uma Tarefa na lista **Execução — Operação e afiliados** (`901114029783`), o PM instruiu explicitamente a verificar os statuses via `clickup_get_task` com `expand_statuses` antes de setar, porque a `clickup-config.md` marcava aquela lista como "não reverificado". A verificação mostrou que o fluxo ali documentado como "anteriormente (não confirmado)" não era apenas incerto — estava **errado em quase todos os nomes**: o doc trazia `to do` / `em desenvolvimento` / `em revisão técnica` / `em homologação` / `aguardando deploy` / `pronto`, e o real é `pendente` → `desenvolvimento` → `bloqueada` → `revisão técnica` → `teste dev` → `deploy p/ alpha` → `teste homolog` → `deploy p/ prd` → `cancelado` (done) / `finalizado` (closed).

Sem retrabalho nem intervenção humana **porque o aviso de "não reverificado" estava no doc e o PM mandou checar** — ou seja, o guardrail funcionou. Não qualifica para o gatilho imediato (`blocker`/`rework`/`human_intervention` todos falsos), mas é correção factual comprovada por leitura direta da API, então o doc foi corrigido na mesma missão em `.claude/` e `.agents/`.

Segundo achado, registrado junto: na mesma lista **existem** `Módulo do PAM` e `KPIs` (ambos gravados e confirmados por releitura do card criado), enquanto o Backlog do **mesmo folder** (`901114029782`) não tem `Módulo do PAM`. Reforça o que a correção de 2026-08-14 já dizia — presença de custom field varia **por lista**, não por folder — e o mapa confirmado dessa lista foi anotado na `clickup-config.md`.

Padrão para o `agente-evolucao` observar: marcar uma seção como "não reverificado" **e deixar o valor antigo errado logo abaixo** convida o agente seguinte a usá-lo quando ninguém mandar checar. Candidato a regra: valor não confirmado é removido ou movido para nota histórica explícita, nunca mantido em linha como se fosse utilizável.

## 2026-09-03 — `priorizado` no Backlog de Delivery dispara a automação de promoção para Execução

```yaml
date: 2026-09-03
mission: especificar
skills: [clickup-spec]
result: artifact
blocker: false
rework: false
human_intervention: true
artifact: https://app.clickup.com/t/868m10vez
```

Ao reestruturar o Bug `868m10vez` (lista **Backlog** de "Delivery: Plataforma", `901114029784`), o status pedido pelo PM era `priorizado` — status válido e existente naquela lista. O `update_task` retornou sucesso, mas a releitura mostrou a task em outra lista: **Execução** (`901114029785`), com status `pendente`. Nenhum `move_task` foi chamado pelo agente.

É a mesma automação já documentada em 2026-08-14 para a lista "Tickets - Tradicional" (folder Clientes, espaço Vertical Loto), que a `clickup-config.md` descrevia como mecanismo de handoff de ticket operacional priorizado para o board de engenharia. O que se confirmou agora é que **o gatilho não é exclusivo daquele folder**: dentro de Vertical Tech, `priorizado` no Backlog de Delivery também move a task fisicamente para Execução, remapeando o status para `pendente` (o pipeline de Execução não tem `priorizado`).

Fast-track aplicado: nota da `clickup-config.md` estendida com o caso confirmado, em `.claude/` e `.agents/`. `human_intervention: true` porque o resultado final diverge do que o PM pediu (o card não ficou em `priorizado`) e a decisão de mantê-lo em Execução ou devolvê-lo ao Backlog com outro status é dele, não do agente.

Implicação de processo, para o `agente-evolucao` observar: `priorizado` funciona como **status de promoção**, não como status de repouso no Backlog. Se o time quiser marcar "priorizado, mas ainda não em execução", esse estado hoje não existe sem disparar a movimentação — vale confirmar com o PM se a automação é intencional para o folder de Delivery ou se é herança da configuração feita para os tickets operacionais.

## 2026-09-17 — `Spike` é um task_type real, não documentado; e dois folders "removidos" ainda aparecem na hierarquia viva

```yaml
date: 2026-09-17
mission: especificar
skills: [clickup-spec]
result: artifact
blocker: false
rework: false
human_intervention: false
artifact: https://app.clickup.com/t/868m69avx
```

Ao criar um desdobramento do card `868m4541a` (Spike de compliance sobre o furo SIGAP na Bravo), o PM pediu para decidir entre `Tarefa` e `Spike` com base no dicionário — mas `clickup-config.md` → "Tipos de Tarefa por Lista" só listava `Epic`/`Débito técnico`/`Bug`/`Tarefa`/`Incidente`/`Correção`, sem `Spike`. O card-pai, porém, tinha `task_type: "Spike"` de verdade (confirmado via `clickup_get_task`) — na mesma lista Sprint 1 do PAM (`901114417832`). Criei o novo card com `task_type: "Spike"` e a API aceitou e persistiu (confirmado por releitura, `868m69avx`), então o tipo existe no workspace e simplesmente não estava documentado. Sem retrabalho — segui direto com o tipo real em vez de forçar `Tarefa` só porque era o único documentado —, mas é correção factual comprovada por escrita+releitura, então apliquei o fast-track: `clickup-config.md` (`.claude/`) ganhou a linha `Spike` na tabela de tipos, com a definição (investigação timeboxed com metodologia incerta, ao contrário de `Tarefa`) e a ressalva de que a presença em outras listas não foi verificada.

Segundo achado, registrado junto: `clickup_get_workspace_hierarchy` no Space (`90114055709`) mostrou que os folders **Strategy** (`90118093876`, lista Objetivos) e **Discovery & Design** (`90118093877`, listas Discovery e Benchmarking) **ainda existem** na hierarquia viva, apesar de `clickup-config.md` afirmar que foram "removidos do ClickUp em 2026-09-10". Não usei nenhum dos dois nesta operação e não investiguei a causa — só anotei a discrepância como aviso no doc (não reescrevi a decisão de 2026-09-10, que pode ter sido só de processo, não de exclusão de fato — nosso próprio guard-rail proíbe excluir folder). Mesma chamada também sugere que o "terceiro folder Sprints" (`90118643577`) marcado como "achado não resolvido" já tem identificação: aparece como Sprint Folder de "Delivery: Produto" (squad Jogos), com 9 sprints numeradas — não confirmei com o PM, só registrei a pista no doc.

Padrão para o `agente-evolucao` observar: a mesma causa-raiz do achado de 2026-09-02 se repete — uma tabela "fonte única" (tipos de tarefa) ficou incompleta porque foi escrita a partir de uma squad/lista só, sem alguém tentar o valor "de fora" antes de descartá-lo. Vale considerar, na consolidação mensal, se `clickup-config.md` precisa de uma varredura ativa de task_types por lista (não só os que o PM já mencionou) antes de declará-la "verificada".

## 2026-09-18 — Skill global desatualizada sobrepôs a do projeto; card real estava no folder errado (squad PAM, não Backoffice)

```yaml
date: 2026-09-18
mission: especificar
skills: [clickup-spec]
result: artifact
blocker: false
rework: false
human_intervention: true
artifact: https://app.clickup.com/t/868kf0wep
```

Missão: corrigir o épico `868kf0wep` (portfólio mal enquadrado, vinculado por engano ao OKR de Receita B2B) e desmembrá-lo em 6 épicos de integração direta de provedor (PGSoft, Pragmatic, Hacksaw, Evolution, Zitro, NGX), todos no folder "Delivery:Backoffice & Integração".

Dois achados, ambos capturados **antes** de criar/alterar qualquer task (sem retrabalho):

1. **Skill desatualizada por duplicidade de instalação.** A chamada `Skill(clickup-spec)` resolveu para `~/.claude/skills/clickup-spec` (global, última alteração de julho/2026) em vez de `/Users/ithaloazevedo/Projetos/pm-loadout/.claude/skills/clickup-spec` (do projeto, atualizado até 09-17). A versão global ainda descreve a hierarquia antiga — Objetivo (OKR/KR) → Iniciativa → Discovery → Delivery, folders "Product Roadmap"/"Discovery & Design" como estrutura viva — tudo removido/depreciado em 2026-09-10 segundo o `clickup-config.md` do projeto. Seguir a versão global teria feito este agente propor uma Iniciativa-pai para os 6 épicos (nível que não existe mais) e reintroduzir Objetivo/Discovery no fluxo. Descartei o conteúdo da chamada `Skill` e seguí direto pelas referências do projeto (`clickup-config.md`, `template-delivery.md`, `clickup-method.md`, `estilo-redacao.md`, datadas 09-10/09-17). **Não é fast-track**: qual cópia deve existir, e por que a resolução do tool pegou a global, é decisão de configuração/infraestrutura de skills — fora do escopo deste agente. Fica para `agente-evolucao`/`agente-governanca` avaliar; risco real, pois qualquer outra sessão que rode `Skill(clickup-spec)` sem cross-checar os arquivos do projeto herda a hierarquia errada.
2. **Doc + suposição do PM divergiam do estado real do card.** O briefing dizia que `868kf0wep` já estava na Backlog do folder "Delivery:Backoffice & Integração". `clickup_get_task` mostrou outra coisa: o card estava na lista "Backlog" (`901114029784`) dentro do folder "Sprints PAM" (`90118303225`) — território da squad PAM, não Backoffice — com status `pronto p/ execução` do fluxo do PAM. Isso só apareceu porque cross-referenciei `clickup_get_task` com `clickup_get_workspace_hierarchy` antes de escrever qualquer coisa; se tivesse confiado só no briefing, o card ficaria fisicamente no folder errado mesmo depois da reescrita de conteúdo. Movi via `clickup_move_task` para `901114029782` (Backlog correto) e resetei o status para `backlog` (o conteúdo foi reescrito do zero; tratar como recém-saído de refinamento pareceu mais seguro que herdar "pronto p/ execução" de um escopo que não existe mais) — sinalizado no handoff para o PM confirmar o reset de status. Achado adicional de estrutura (não solicitado pela missão, mas descoberto no meio dela): o folder "Sprints — PAM" está aninhado dentro de "Delivery: PAM", não é irmão no nível do Space como o `clickup-config.md` descrevia — fast-track aplicado, nota anexada ao doc (`.claude/` e `.agents/`) sem reescrever a tabela de Hierarquia (mudar o modelo documentado é decisão estrutural).
3. Achado menor, mesma missão: `868kf0wep` não tinha nenhum assignee no momento da leitura, apesar do briefing afirmar "Assignee atual é Ícaro Lopes". Não toquei no campo (instrução era não alterar assignee), só sinalizei a divergência no handoff.

`human_intervention: true` porque (a) o PM precisa confirmar se o reset de status para `backlog` foi a escolha certa, e (b) a divergência do folder aninhado "Sprints PAM"/"Delivery: PAM" exige confirmação antes de qualquer doc tratar isso como modelo resolvido — não é correção que um agente operacional deveria fechar sozinho.

Padrão para o `agente-evolucao` observar: dois achados nesta missão são variações do mesmo problema já registrado em 2026-09-02 e 2026-09-17 — doc "fonte única" que fica defasada porque ninguém tenta o caminho "de fora" antes de confiar no texto. A novidade aqui é que a fonte defasada não era só um arquivo de referência, era a **skill inteira**, e havia duas cópias instaladas (`~/.claude` global vs. projeto) sem mecanismo visível de por que uma ganha da outra. Vale investigar se isso acontece em outras skills do usuário, não só `clickup-spec`.
